-- =============================================================
-- Implementation Tracker — เพิ่มคอลัมน์ source_project_id ใน impl_projects
-- ใช้เชื่อมโยงกลับไปยังโครงการต้นทาง (ตาราง projects / window.PROJECTS)
-- เพื่อให้หน้า "แก้ไขโครงการ" ดึงวันที่เริ่ม-สิ้นสุด/เจ้าของไซต์/ผู้ติดตั้ง
-- จากโครงการต้นทางมาแสดงแบบอ่านอย่างเดียว และอัปเดตอัตโนมัติเมื่อสลับโครงการ
-- รันครั้งเดียวใน Supabase Dashboard → SQL Editor (ปลอดภัย รันซ้ำได้ ไม่กระทบข้อมูลเดิม)
-- =============================================================
ALTER TABLE impl_projects ADD COLUMN IF NOT EXISTS source_project_id TEXT DEFAULT '';

-- โครงการที่สร้างไว้ก่อนหน้านี้จะไม่มีค่านี้ (เป็น '') — แอปจะเดาโครงการต้นทางให้อัตโนมัติ
-- จากชื่อที่ตรงกันพอดีตอนเปิดฟอร์ม "แก้ไขโครงการ" ครั้งแรก ไม่ต้องรัน backfill เพิ่ม
